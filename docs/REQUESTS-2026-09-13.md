# Portfolio app listing request, 2026-09-13

User request: properly list all apps in the designated space on arnavgoel.dev.

- [done] Audit existing app/studio entries against Atlas and app source/release records.
- [done] Name Halation and Emulsion explicitly with accurate platform/development status.
- [done] Refresh the released Quiver app versions and canonical studio links.
- [done] Add Saycut as the verified iOS working prototype; no release/download links.
- [done] Keep project pages, search and llms indexes consistent; record every changed path.
- [done] Run lint, typecheck, index gate and production build. Initial build hit ENOSPC, retry passed after task-cache cleanup.
- [blocked] Rendered-page verification: Firecrawl credits exhausted, browser runtime has zero browsers, normal headless Chrome aborts exit -6. No bypass flags used.
- [done] Commit/push through the existing Vercel hosting workflow and verify live content. Application commit `204b5a0`, Vercel production `dpl_B3fGJ2ZjYCRVxKy8BQMbVXMSygzQ`, Ready and aliased to arnavgoel.dev.

Sources: portfolio docs/STATE.md, Atlas app inventory, Halation README/current native and Android target records, Quiver suite.config.json and version files, Saycut README and docs/ROADMAP.md. Manifest is a city-data pipeline and renderer, not a verified missing consumer-app listing in this bounded app audit.

Browser/credit status: Firecrawl scrape refused because account credits are exhausted. The browser runtime reports no available browsers. Alternative rendered verification remains to be established; these failures are not a page defect.


Changed paths:
- `src/lib/projects.ts`: app names, verified versions/platforms, canonical Quiver URL, Saycut prototype, removal of obsolete anonymous sibling and signing claims.
- `src/components/command-palette.tsx`: canonical app/surface search keywords and one shared section order.
- `public/llms.txt`, `public/llms-full.txt`: matching app listings and signing correction.
- `.gitignore`: exclude local `.firecrawl` verification evidence.
- `docs/STATE.md`, this file: source audit, validation and outstanding visual review.

Evidence: live portfolio/studio HTTP 200 responses and three GitHub release API
responses are saved under ignored `.firecrawl/`. Existing public screenshots are
retained; no private user camera images were published. Manifest is a city data
pipeline/renderer, not a verified missing consumer app in this bounded audit.

Generated HTML checked with script/style text excluded: index names Emulsion and Saycut; studio detail has all three current versions; photography detail names both products with platforms and no store links; Saycut says Working prototype. The first check exposed the detail template omitting eyebrow copy, so its existing prototype wording was added to the description and the build/check repeated successfully. This is content verification, not a visual screenshot.


Published native app inventory: Trove, Relay, Tend (Quiver); Halation and
Emulsion (shared photography project); Saycut (iOS working prototype); Buzz
(existing SwiftUI iOS/macOS app in progress). Watch Together remains listed as
its browser extension, including Safari. Existing web apps/studios remain in
the same Projects directory. No verified native consumer app in this source
inventory remains omitted; Manifest is a city data pipeline/renderer and was
not promoted into a consumer app. Android Saycut is planned, not implemented,
and is not listed as an available platform.

Production receipt: application commit `204b5a0`, Ready deployment
`dpl_B3fGJ2ZjYCRVxKy8BQMbVXMSygzQ`, https://arnavgoel.dev. Live checks passed
for `/projects`, `/projects/studio`, `/projects/halation`, `/projects/saycut`,
`/llms.txt` and `/llms-full.txt`, with HTML script/style content excluded.
Outstanding: rendered desktop/mobile review requires an available browser.


## Project preview overlap reported 2026-09-13

- [done] Move all project previews outside animated section containment using the existing Base UI Dialog portal, backdrop, viewport and popup.
- [done] Bound the popup to dynamic viewport height, contain inner scrolling, and use library modal focus/scroll/dismissal handling. Added an accessible title and close control.
- [done] Lint, TypeScript, content-index gate and production build passed without warnings. No dependencies added.
- [blocked] Rendered verification of viewport bounds, background blocking, scrolling and dismissal: browser runtime lists zero browsers; standard headless shell fails with Mach port registration denied, exit -5. Evidence: `.firecrawl/modal-overlap/browser.log`.
- [done] Published application commit `00feacb`: production deployment `dpl_BTa5bft2E2pJcZd8KUELaYQcDLCZ` is Ready and aliased to arnavgoel.dev. Live `/projects` returned HTTP 200; referenced assets contain the portal, dynamic viewport height, scroll containment and close label. Receipt: `.firecrawl/modal-overlap/live-verification.json`. This does not replace rendered verification.
- Prior native app installation/device-test and portfolio visual limitations remain open.

Changed paths for this fix: `src/components/project-card.tsx`, `docs/STATE.md`
and this request ledger. The modal contents retain their existing wording and
styling; indentation follows the new dialog nesting. The CSS animation remains
on page sections, while the modal is portalled to the document body.


## Completion retry, 15 September 2026

- [done] Reconnect the supported browser runtime: discovery returned an empty list.
- [done] Calibrate the ordinary installed headless browser on `about:blank`: exit -5,
  Mach port registration denied (1100), no screenshot. No bypass flags used.
- [done] Recheck Vercel production: Ready, deployment
  `dpl_Fn7AZYgnFWH3zC1a3zQypEbJjG3R`, aliased to arnavgoel.dev.
- [blocked] Rendered desktop/mobile review, bottom-of-page opening, inner scroll,
  background blocking, Escape/backdrop dismissal and focus behavior need an
  accessible browser. The current failure occurs before the page loads.

Only `docs/STATE.md` and this ledger changed. Raw evidence is in ignored
`.firecrawl/modal-overlap-20260915/`. This retry does not establish a visual pass
or close the native app/device requests owned by the Halation repository.
